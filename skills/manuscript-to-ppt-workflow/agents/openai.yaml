interface:
  display_name: "Manuscript to PPT Workflow"
  short_description: "Confirmed-brief workflow for research PPT decks"
  default_prompt: "Use $manuscript-to-ppt-workflow to start with codex-deep-interview, confirm the production brief, ground facts, delegate storyboard and PPTX automation to native agents, and fail the run if render QA finds layout defects."
policy:
  allow_implicit_invocation: true
