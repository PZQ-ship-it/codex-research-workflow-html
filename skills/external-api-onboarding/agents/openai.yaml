interface:
  display_name: "External API Onboarding"
  short_description: "Configure auth safely and audit fallback drift"
  default_prompt: "Use $external-api-onboarding to configure a provider API key, MCP login, .env storage, fallback audit, and smoke test without exposing secrets."
