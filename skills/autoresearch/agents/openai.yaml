interface:
  display_name: "Autoresearch"
  short_description: "Run validator-gated native research"
  default_prompt: "Use $autoresearch to run this research task until an explicit validator passes."
