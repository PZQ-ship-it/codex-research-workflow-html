interface:
  display_name: "Paper Repro Source Lock"
  short_description: "Lock official code data model sources"
  default_prompt: "Use $paper-repro-source-lock to lock the official paper, code, dataset, model, and provenance sources for this reproduction."
