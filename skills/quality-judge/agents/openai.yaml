interface:
  display_name: "Quality Judge"
  short_description: "Task-first review gates with calibrated anchors"
  default_prompt: "Use $quality-judge to run a task-first dual-review gate with calibrated anchors and a verified challenge frontier."
policy:
  allow_implicit_invocation: false
