interface:
  display_name: "PPT Render QA Loop"
  short_description: "Render PPTX pages and block bad decks"
  default_prompt: "Use $ppt-render-qa-loop to render the PPTX, report visual and academic QA defects, write a repair backlog when needed, and stop for acceptance or repair."
