interface:
  display_name: "PPT Deck Build"
  short_description: "Build editable PPTX drafts from frozen plans"
  default_prompt: "Use $ppt-deck-build to generate an editable PPTX draft from confirmed upstream artifacts, then stop before render QA."
