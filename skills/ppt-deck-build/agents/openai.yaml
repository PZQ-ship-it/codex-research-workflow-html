interface:
  display_name: "PPT Deck Build"
  short_description: "Plan and run page-worker PPTX builds"
  default_prompt: "Use $ppt-deck-build to create a page-worker deck build work order from confirmed upstream artifacts, stop for confirmation, then run one slide worker per page only after I approve dispatch."
